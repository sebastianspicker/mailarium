# Embedding & Vector DB
usearch==2.26.0
torch==2.13.0
sentence-transformers==5.6.0
transformers==5.13.0

# MCP Server
mcp[cli]>=1.28.1
pydantic>=2.0.0

# Email parsing
lxml>=6.1.0

# Analysis
networkx>=3.2
pandas>=2.1.0

# Search quality
rank_bm25>=0.2.2

# Reporting
jinja2>=3.0.0

# NLP
textstat>=0.7.0

# PDF export (optional; exporter falls back to HTML without it)
# weasyprint>=62.0

# Utilities and UI
python-dotenv>=1.0.0
rich>=13.0.0
streamlit>=1.40.0

# Security: minimum versions for transitive deps with known CVEs
# PyJWT security floor (required by mcp)
PyJWT>=2.13.0
# Tornado security floor (required by streamlit)
tornado>=6.5.7
# Cryptography security floor (required by multiple dependencies)
cryptography>=48.0.1
# pillow security floor (required by transformers/sentence-transformers)
pillow>=12.3.0
# pygments CVE-2026-4539, fixed in 2.20.0 (required by rich)
pygments>=2.20.0
# python-multipart security floor (required by mcp)
python-multipart>=0.0.31
# requests CVE-2026-25645, fixed in 2.33.0 (required by multiple deps)
requests>=2.33.0
